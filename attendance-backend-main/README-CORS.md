# CORS setup

Backend uses `allowedOrigins` in `server.ts`.

To allow the frontend at `http://localhost:5173`, ensure `server.ts` includes it.

Current allowed origins includes `http://localhost:5173`.

