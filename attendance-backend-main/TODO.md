# TODO (Face biometric auto-capture wiring)

- [x] Wire `FaceVerify` into `E-visitors-main/src/pages/dashboard/attendance/ScanningPage.tsx`
- [x] When `selectedMode === 'face'`, render the face verifier component automatically
- [x] On successful face match, trigger check-in flow automatically (auto-checkin)
- [x] Add guard to avoid repeated check-ins on the same match
- [ ] Validate build/dev run


