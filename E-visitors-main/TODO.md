# TODO - Self Registration Kiosk UI

- [ ] Refactor `SelfRegistrationCard.tsx` details step UI into two sections (Left/Right columns) so all fields are visible without scrolling.
- [ ] Refactor `host` step UI into two sections (Left/Right columns) with host search/list on one side and appointment fields on the other.
- [ ] Refactor `confirmation` step UI into two sections (Left/Right columns) while keeping QR/pass card on right.
- [ ] Ensure layout uses fixed-height panels + `overflow-hidden` / `overflow-auto` only where needed, so kiosk screen doesn’t require page scroll.
- [ ] Run frontend typecheck/build if available and ensure no TSX errors.

