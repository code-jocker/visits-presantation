# TODO - Face recognition self registration

- [x] Inspect existing face flow (frontend capture + backend verify stub)
- [ ] Create a plan for descriptor-based recognition
- [ ] Implement descriptor computation in browser (face-api.js) OR backend (confirm)
- [ ] Add backend endpoints to register/store face descriptors and verify via distance
- [ ] Update DB models to persist embeddings
- [ ] Update frontend to send descriptors instead of base64 images for matching
- [ ] Run build/tests and validate kiosk end-to-end

